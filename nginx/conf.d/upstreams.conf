# Upstreams — adjust to where your services actually live.
# If services run on this VM over Tailscale IP 100.80.89.81, point there.
# Examples below assume HTTPS for Nextcloud, HTTP for Jellyfin (adjust as needed).

upstream nextcloud_upstream {
    server 100.80.89.81:8443;  # CHANGE if different
}

upstream jellyfin_upstream {
    server 100.80.89.81:8096;  # CHANGE if different
}
